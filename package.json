{
  "name": "aegis-firewall-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "TypeScript monorepo for the aegis-firewall npm package.",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "packageManager": "pnpm@9.7.0"
}
